<Controls:MetroWindow 
        x:Class="PrimeraValdivia.Views.FormularioEvento"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:PrimeraValdivia.Views"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:viewModels="clr-namespace:PrimeraValdivia.ViewModels"
        DataContext="clr-namespace:PrimeraValdivia.ViewModels.EventoViewModel"
        mc:Ignorable="d"
        GlowBrush="Black"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">

    <TabControl>
        <TabItem Header="Datos Comunes">
            <Grid Background="#FFE5E5E5" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Label Content="Formulario sobre atributos de la tabla Evento" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF1B1B1B"/>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Header="Datos Incendio/Rescate">
            <Grid Background="#FFE5E5E5" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Label FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF1B1B1B">
                        Formulario sobre atributos de la tabla incendio o rescate
                        Formulario sobre atributos de las tablas: 
                        - ERA 
                        - Motosierra 
                        - Material evento 
                        - Material mayor
                    </Label>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Header="Detalles">
            <Grid Background="#FFE5E5E5" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Label FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF1B1B1B">
                        Incendio: Formulario sobre atributos de las tablas: 
                        - Material peligroso 
                        - Gas 
                        - Apoyo 
                        - Afectado incendio 
                        - Aseguradora vivienda afectado 
                        Rescate: Formulario de atributos de las tablas: 
                        - Afectado rescate 
                        - Vehiculo 
                        - Aseguradora vehiculo 
                        - Acompañante
                    </Label>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Header="Asistencia">
            <Grid Background="#FFE5E5E5" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Column="0" Grid.Row="1">
                    <DataGrid CanUserAddRows="True" ItemsSource="{Binding Voluntarios}" SelectedItem="{Binding Voluntario}" AutoGenerateColumns="False"
                              CanUserDeleteRows="True" CanUserReorderColumns="True" Grid.Row="1"
                              CanUserSortColumns="True" Margin="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="rut" Binding="{Binding Path=rut, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="nombre" Binding="{Binding Path=nombre, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="cargo" Binding="{Binding Path=cargo, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                <Grid Grid.Column="1" Grid.Row="1">
                    <DataGrid CanUserAddRows="True" ItemsSource="{Binding Asistentes}" SelectedItem="{Binding Voluntario}" AutoGenerateColumns="False"
                              CanUserDeleteRows="True" CanUserReorderColumns="True" Grid.Row="1"
                              CanUserSortColumns="True" Margin="10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="rut" Binding="{Binding Path=rut, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="nombre" Binding="{Binding Path=nombre, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="cargo" Binding="{Binding Path=cargo, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                <Grid Grid.Column="0" Grid.Row="0" Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" VerticalAlignment="Center"/>
                    <Button Content="Buscar" Grid.Column="1"/>
                </Grid>
            </Grid>
        </TabItem>
        
    </TabControl>
</Controls:MetroWindow>

