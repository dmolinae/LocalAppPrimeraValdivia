<UserControl x:Class="PrimeraValdivia.Views.Voluntarios"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrimeraValdivia.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0"
            ItemsSource="{Binding Voluntarios}"
            AutoGenerateColumns="True" Margin="10">
        </DataGrid>
        <TextBlock Grid.Row="1" Text="{Binding rut}"/>
    </Grid>
</UserControl>
